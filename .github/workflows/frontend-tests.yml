# ⚠️ DEPRECATED - This workflow has been replaced by ci.yml
# Please use .github/workflows/ci.yml for all CI checks
# This file is kept for reference only and will be removed in the future

# .github/workflows/test.yml
name: Run Tests (DEPRECATED)

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Prettier Check
        run: npm run format:check

      - name: Run Tests with Coverage
        run: npm run test:coverage

      - name: Upload Coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }} # optional for public repos
